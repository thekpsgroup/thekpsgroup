---
// Phoenix, AZ - High Opportunity Market
import CityPageTemplate from '../../../components/templates/CityPageTemplate.astro';
import { MAJOR_CITIES } from '../../../data/cities';

const cityData = MAJOR_CITIES.find(city => city.name === 'Phoenix');
if (!cityData) {
  throw new Error('Phoenix city data not found');
}
---

<CityPageTemplate city={cityData} />
