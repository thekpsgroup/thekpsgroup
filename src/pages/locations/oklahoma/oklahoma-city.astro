---
// Oklahoma City, OK - High Opportunity Market
import CityPageTemplate from '../../../components/templates/CityPageTemplate.astro';
import { MAJOR_CITIES } from '../../../data/cities';

const cityData = MAJOR_CITIES.find(city => city.name === 'Oklahoma City');
if (!cityData) {
  throw new Error('Oklahoma City city data not found');
}
---

<CityPageTemplate city={cityData} />
